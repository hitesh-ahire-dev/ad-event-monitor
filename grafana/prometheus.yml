global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: ad-event-monitor
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["localhost:8080"]
        labels:
          application: ad-event-monitor

rule_files:
  - /etc/prometheus/alerts.yml
